# Track B-2: ViT·YOLOv8 — 실습 과제

> 실습 노트북을 실행한 후, 동일한 노트북 안에 새 셀을 추가하여 과제를 완성하세요.
> **새 라이브러리 설치 불필요 | 과제당 소요 시간: 약 15~20분**

---

## 📒 노트북 01 — ViT 기초

**과제 A (필수)**
실습에서 사용한 이미지 외에 **다른 이미지 1장**을 ViT로 분류하고 결과를 출력하세요.

**과제 B (필수)**
224×224 이미지를 16×16 패치로 나누면 몇 개인지, 32×32로 나누면 몇 개인지 계산하세요.

```python
print(f"16x16 패치: {(224//16)**2}개")
print(f"32x32 패치: {(224//32)**2}개")
```

**과제 C (심화·선택)**
분류 결과에서 상위 3개 클래스와 확률을 출력하세요.

```python
top3 = logits.softmax(-1).topk(3)
for score, idx in zip(top3.values[0], top3.indices[0]):
    print(f"{model.config.id2label[idx.item()]}: {score.item():.3f}")
```

---

## 📒 노트북 02 — ViT 전이학습

**과제 A (필수)**
`learning_rate=2e-4`를 `1e-4`로 바꿔 실행(또는 결과 비교)하고 Recall 변화를 출력하세요.

**과제 B (필수)**
Trainer의 log_history에서 train loss 값을 꺼내 꺾은선 그래프로 그리세요.

```python
logs = trainer.state.log_history
train_loss = [x['loss'] for x in logs if 'loss' in x]
plt.plot(train_loss, label='Train Loss')
plt.title('학습 곡선')
plt.legend()
plt.show()
```

**과제 C (심화·선택)**
`classification_report`로 클래스별 recall을 출력하고 가장 낮은 클래스를 찾으세요.

---

## 📒 노트북 03 — YOLOv8 파인튜닝

**과제 A (필수)**
`model.predict(conf=0.5)`를 `conf=0.3`으로 바꿔 탐지 결과가 어떻게 달라지는지 확인하세요.

**과제 B (필수)**
탐지 결과 이미지를 불러와 plt로 표시하세요.

```python
from PIL import Image
result_img = Image.open(results[0].save_dir + '/image0.jpg')
plt.imshow(result_img)
plt.axis('off')
plt.show()
```

**과제 C (심화·선택)**
detection_signal.json에서 defect_rate를 읽어 HIGH/MEDIUM/NORMAL을 출력하세요.

---

## 📒 노트북 04 — Streamlit 대시보드

**과제 A (필수)**
`streamlit run src/app.py`를 실행하고 대시보드 화면을 스크린샷으로 저장하세요.

**과제 B (필수)**
드롭다운에서 YOLOv8n과 YOLOv8s를 각각 선택하여 결과 차이를 확인하세요.

---

## 📌 제출 방법

- 기존 노트북에 과제 셀을 추가하고 전체 실행 후 `.ipynb` 파일 제출
- **제출 기준**: 과제 A·B 실행 완료 + 결과 확인 + 관찰 주석 1줄
